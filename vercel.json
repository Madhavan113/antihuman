{
  "buildCommand": "pnpm --filter @simulacrum/types build && pnpm --filter @simulacrum/ui exec -- vite build",
  "outputDirectory": "dist",
  "installCommand": "pnpm install",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
